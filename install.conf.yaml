- clean: ['~']
- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.Xresources: Xresources
    ~/.gitconfig: gitconfig
    ~/.inputrc: inputrc
    ~/.i3: i3/
    ~/bin: bin/
    ~/.vimrc: vimrc
    ~/.vim: vim/
    ~/.quiltrc: quiltrc
- shell:
    - git submodule update --init --recursive
    - mkdir -p ~/.vim/backup
    - mkdir -p ~/.vim/swap
    - mkdir -p ~/.vim/undo

